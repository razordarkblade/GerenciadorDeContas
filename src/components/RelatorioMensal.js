import React, { Component } from 'react'
import { View, Text, StyleSheet, FlatList } from 'react-native'

export default class RelatorioMensal extends Component {
    render() {
        return(
            <View>
                <Text>Relatório Mensal</Text>
            </View>
        )
    }
}